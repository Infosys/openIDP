<?xml version="1.0" encoding="UTF-8"?>

<project name="$PNAME" basedir=".">

	<target name="jar" >
		<mkdir dir="$JARDIR">
		</mkdir>
		<jar jarfile="$JARFOLDER" basedir="$BIN" >
			<manifest>
				<attribute name="Main-Class" value="$MAINCLASS">
				</attribute>
			</manifest>
		</jar>
	</target>

</project>
